import React, { useState } from 'react';
import type { userDocsArrayType } from '../../App';

interface DocLinkerTypes {
    userDocs: userDocsArrayType
}

const DocLinker = ({ userDocs }:DocLinkerTypes) => {

};

export default DocLinker;
